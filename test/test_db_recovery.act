import time

actor main(env):
    var i = 0;

    def _work():
        if i == 4:
            await async env.exit(0)
        after 1: _work()
        i += 1
        print("COUNT:", i, int(time.time()))

    _work()
