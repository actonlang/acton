[package]
name = "test_doc_printing"
version = "0.1.0"

[dependencies]
# No external dependencies needed for documentation tests

[dev-dependencies]
# Development dependencies if needed

[zig-dependencies]
# Zig dependencies if needed