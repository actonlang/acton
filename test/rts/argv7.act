actor main(env):
    # Never expected to get here as we should be invoked with invalid command
    # line argument. The test actually expects a failure, so we exit with 0 to
    # get a test failure.
    env.exit(0)
