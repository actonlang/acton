import shared

def check(backend: int, page_hash_log2: int, incremental: bool) -> int:
    NotImplemented

actor main(env):
    records = shared.make()
    assert check(int(env.argv[1]), int(env.argv[2]), env.argv[3] == "true") == 0
    assert shared.verify(records) == 256
    print("GC tuning OK")
    env.exit(0)
