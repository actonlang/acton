import shared

def check(backend: int, incremental: bool) -> int:
    NotImplemented

actor main(env):
    records = shared.make()
    assert check(int(env.argv[1]), env.argv[2] == "true") == 0
    assert shared.verify(records) == 256
    print("GC tuning OK")
    env.exit(0)
