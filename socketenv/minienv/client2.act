import minienv

actor client(env):

    def handler1(str):
        env.stdout_write(str)
        
    def session(conn):
        conn.on_receipt(handler1, handler1)

        def handler2(str):
            conn.write(str)

        env.stdin_install(handler2)

    env.connect("localhost",12345,session)
