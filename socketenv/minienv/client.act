import minienv

actor client(env):

    def session(conn):
        if conn is not None:
            conn.on_receipt(input = env.stdout_write,error = env.stdout_write)
            env.stdin_install(conn.write)
        else:
            env.stdout_write("Connection failed")

    env.connect("localhost",12345,session)
